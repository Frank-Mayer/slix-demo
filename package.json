{
  "scripts": {
    "dev": "parcel index.html --open",
    "build": "parcel build index.html --no-source-maps --no-cache",
    "publish": "parcel build index.html --no-source-maps --no-cache --public-url https://frank-mayer.github.io/slix-demo"
  },
  "dependencies": {
    "@frank-mayer/slix": "^1.0.10",
    "framer-motion": "^7.5.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "parcel": "^2.7.0",
    "process": "^0.11.10",
    "typescript": "^4.8.4"
  }
}
